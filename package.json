{
  "name": "debugger",
  "main": "./lib/index",
  "author": "AtomDebugger",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "atom": ">=1.9.0 <2.0.0"
  },
  "scripts": {
    "test": "(apm test) && (flow check) && (eslint . )"
  },
  "keywords": [
    "debugger"
  ],
  "description": "Base debugger with Cow Powers",
  "dependencies": {
    "atom-package-deps": "^4.0.0"
  },
  "devDependencies": {
    "eslint-config-steelbrain": "^1.0.4",
    "flow-bin": "^0.31.1",
    "jasmine-fix": "^1.0.1"
  },
  "package-deps": [
    "debugger-ui-default"
  ],
  "providedServices": {
    "debugger": {
      "versions": {
        "1.0.0": "provideDebuggerRegistry"
      }
    }
  },
  "consumedServices": {
    "debugger-ui": {
      "versions": {
        "^1.0.0": "consumeUI"
      }
    }
  }
}
